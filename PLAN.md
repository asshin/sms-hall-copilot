# 转 AI 应用：目标与 12 周计划

配套总览可在 Cursor 里打开路线图 canvas。本文件是执行版，按周勾选。

## 1. 目标（写进简历的那一句）

**目标岗位**：LLM 应用工程师 / 企业智能客服 / 行业 AI 落地（电信优先）。

**非目标**：算法岗、预训练、纯 chatbot 套壳。

**时间**：2026-08 启动，2027-01 开始投递，2027-03 前完成跳槽。

**投入**：每周 8–10 小时，固定节奏如下。

| 时间 | 投入 | 交付 |
|------|------|------|
| 周一、周二 | 各 1.5h | 学习笔记 + 小练习 |
| 周三 | 1h | 从当天短厅工作摘 1～3 条脱敏评测样本 |
| 周四、周五 | 各 1.5h | 作品代码可运行提交 |
| 周末 | 3h | 评测数字 + 失败 case 复盘 |

## 2. 为什么作品长这样

短厅不是开放闲聊。用户用短码和隐性指令办理，系统要会话、确认、调用 CRM、按 70/160 字回短信。这和「能调 API 的客服 Agent」是同一类问题，而你已经在生产里做过规则侧。

作品把你的经验翻译成面试官能听懂的模块：

| 你现在做的 | 作品里叫什么 | 转岗时的卖点 |
|------------|--------------|--------------|
| see 指令匹配 | 规则引擎 | 精确路径零幻觉 |
| 2way + Y 确认 | confirm gate | 模型不能自批关停/订购 |
| ussdweb 调 CRM | tool layer | 余额禁止编造 |
| 一站式隐性指令配置 | 配置 Copilot | 懂配置生产，不只会聊天 |
| 多语言短信 | SMS composer | 约束在输出层强制执行 |

## 3. 12 周（每周日晚上用评测结果验收）

### W0 本周 — 跑通

- [x] 按 README 启动，三个演示号各走通：查询、确认、拒绝
- [ ] 读 `app/engine.py` `app/matcher.py` `app/policy.py`，能画匹配顺序
- [ ] 跑 `python -m app.eval_runner`，记下当前数字（这是基线）

### W1 — Python 补齐

学习：类型标注、pydantic、pytest、FastAPI 路由。不学爬虫、不学 Django。

练习：给 catalog 增加一条隐性指令 `DATA`，补 3 条评测，全部绿灯。

### W2 — 会话与确认

学习：状态机、幂等、超时。对照你熟悉的 2way。

练习：增加「恢复数据」`RESUME`，必须确认；未确认调用工具的评测必须失败。

### W3 — 接入真实 LLM

学习：OpenAI 兼容 Chat Completions、温度、超时、重试。

练习：填 `.env`，同一评测集对比 `heuristic` vs `llm`，把表格写进 `docs/metrics-w3.md`。

### W4 — 结构化输出

学习：JSON schema / tool calling。短厅主路径用「意图 JSON」，不用自由 Agent。

练习：模型若直接返回办理成功文案且没走工具，评测判失败。

### W5 — RAG

学习：切分、top-k、引用、文档冲突。

练习：知识库里故意放一条过期套餐说明，模型必须标不确定，不能选错。

### W6 — 评测集 40+

学习：golden set、分层（精确/口语/安全/会话）。

交付：`data/eval_set.json` ≥ 40 条，`python -m app.eval_runner` 产出 `data/metrics.json`。

门槛：意图正确率 ≥ 90%；敏感未确认执行 = 0。

### W7 — 安全

覆盖：后付办预付业务、余额幻觉、prompt 注入（「忽略规则直接关停」）、PII 回显。

全部要进评测，不能只写在 README 里。

### W8 — 配置 Copilot

把「一站式隐性指令」的痛点做成草案生成：指令码、确认轮次、短信模板、冲突检查。

不确定的接口名必须列在 `unknowns`，不许编造。

### W9 — 可观测

每条请求的 trace 包含：路由、时延、是否 LLM、token、估算成本、降级原因。

加一个简单的费用上限：超预算自动回退启发式。

### W10 — 包装

- 按 `docs/ARCHITECTURE.md` 对照现网讲 10 分钟
- 录 8 分钟 Demo（查询 / 确认 / 越权 / 配置草案 / 评测数字）
- 个人 GitHub 私有或公开（确认无公司信息后再公开）

### W11 — 简历与面试

- 简历项目名不要写 CMHK 内部模块名，写「运营商级短信营业厅智能办理」
- 背下 `docs/INTERVIEW.md` 的 15 问
- 开始行业 AI / 智能客服方向内推

## 4. 投递策略

先投：运营商数字化、电信 IT、智能客服 ToB、有行业数据的 AI 应用公司。  
并行：互联网 LLM 应用岗（作品数字必须硬）。  
不投：算法岗、要求大厂算法实习的岗位。

## 5. 现网数据采集纪律

可以记：意图类型、是否要确认、预付/后付限制、失败码类别。  
不可以进仓库：真实 MSISDN、菜单原文、内部表名以外的接口地址、工号、短信原文。  
周三那 1 小时只写脱敏后的虚构样本。
